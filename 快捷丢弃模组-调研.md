---
tags: [spt, mod, 模组, 丢弃, discard, inventory, dragdrop, 调研]
updated: 2026-09-08
status: 调研完成-待决策
---

# 快捷丢弃模组 — 调研归档

> 需求：战局内装备界面做「丢弃区」，物资拖入该区域立即丢弃。
> 主人当前把局内丢弃绑成 E 键（原版 `ECommand.ThrowItem`），但 E 还有其他用途，想换更稳的交互。
> 环境：SPT 4.1.x (EFT build 40743) @ `D:\free games\EFT_0821\`，Fika 联机。
> 调研日期：2026-09-08。**方案未定，主人下次来要重新提问决策。**

## 一、原版丢弃机制调用链（已本地+反编译双重验证）

```
右键菜单[丢弃] / ECommand.ThrowItem 按键命令
  → ItemUiContext.TranslateCommand 收到 ECommand.ThrowItem
  → GetItemContextInteractions(...).ExecuteInteraction(EItemInfoButton.Discard)   # CanDiscard 检查
  → ItemUiContext.ThrowItem(Item item)                    # async Task，内含可能确认框
  → InteractionsHandlerClass.Discard(item, itemController)
      ├ 检查 HasLockedContent（任务/锁定不可丢）
      ├ Remove: 从格子移除 + 解绑(unbind) + 清标签(smethod_8/smethod_6)
  → 经 controller.TryRunNetworkTransaction → raid 玩家权威 + Fika 同步
```

### 关键 API（本地 Assembly-CSharp.dll 40743 已 dump 验证）

| API | 类 | 用途 |
|---|---|---|
| `ThrowItem(Item)` | `EFT.UI.ItemUiContext` (158 methods) | **UI 丢弃入口，右键同款；走标准事务 → Fika 同步安全** |
| `CanDiscard` | `EFT.UI.BaseItemContextInteractions` | 可丢性检查（右键菜单灰显用） |
| `Discard / DiscardWithoutRestrictions` | `EFT.InventoryLogic.ItemManipulator` | 丢弃/无视限额丢弃 |
| `Discard(item, controller, simulate)` | `InteractionsHandlerClass` (静态工厂) | 生成 GStruct455<GClass3200> 操作 |
| `IsUndiscardable / AddDiscardLimits / HasDiscardOverLimit / ResetDiscardLimits` | `PlayerInventoryController` | Anti-RMT 丢弃限额 |
| `IAntiRMTItemController` | `EFT.InventoryLogic` | 限额接口 |
| `ApplyThrowItemAsLoot` | `EFT.NextObservedPlayer.ObservedPlayerInventoryController` | Fika host 把丢弃物转地上 loot |
| `AffectsDiscardLimits / HasDiscardLimits` | `EFT.InventoryLogic.InventoryExtension` | 扩展 |
| `DiscardLimitsReachedError / DiscardLimitsDesyncError / DevelopResetDiscardLimitsPacket` | InventoryLogic / EFT | 限额错误与重置包 |
| `MIN/MAX_TIME_DISCARD_AIM_SEC, DAMAGE_TO_DISCARD_AIM_0_100` | 常量 | 丢弃瞄准相关（机制未深挖） |

## 二、拖拽系统（丢弃区可行性核心）

EFT 0.16 物品拖拽 = **标准 Unity EventSystem**（DragTrigger/UIDragComponent 实现 IDragHandler），自定义丢弃区可以用常规 uGUI 方式实现。

| 类 | 关键成员 | 用途 |
|---|---|---|
| `EFT.UI.DragAndDrop.ItemView` (88) | `OnBeginDrag/OnDrag/OnEndDrag`、`get_IsBeingDragged`、`get_Item`/`get_ItemContext`、`get_DraggedItemView`、`CheckAcceptHandler` | **松手检测点** |
| `EFT.UI.DragTrigger` | OnDrag/OnBeginDrag/OnEndDrag + add_/remove_ 事件 | 全局拖拽事件（UI Fixes 直接 patch 这里） |
| `EFT.UI.UIDragComponent` | `UnityEngine.EventSystems.IBeginDragHandler.OnBeginDrag` 等 | 同上 |
| `GridView` (43) | `CanAccept`、`AcceptItem`、`HighlightItemViewPosition(DragItemContext itemContext...)`、`GetTargetItem`、`DragStarted`、`DragCancelled` | 拖拽中拿 DragItemContext 的现成管道 |

### 推荐实现骨架（借鉴 UI Fixes / Discarder 已验证模式）
1. Harmony patch `ItemView.OnEndDrag`（或 DragTrigger.OnEndDrag）：松手时用 `RectTransformUtility.ScreenPointToLocalPointInRectangle` 判断光标是否落在丢弃区 rect 内
2. 丢弃区 UI：挂到局内 InventoryScreen 底部（RaidInventoryScreenController 场景），半透明 Image + 红色高亮，拖入时变色
3. 命中则 `ItemUiContext.ThrowItem(item)` —— **不要**手搓低层 Remove，否则 Fika desync
4. 确认框存在则 patch `ShowMessageWindow` 短路（Discarder 的做法）

## 三、风险点

1. **Anti-RMT discard limits**：0.16 客户端有限额（_discardedItemsCache），但 **SPT server-csharp 无任何 discard handler**（gh 代码搜索=0 命中）→ 单机无碍；raid 丢自己背包物品走 Remove 基本不碰限额。真被卡 → `DiscardWithoutRestrictions` 或 `ResetDiscardLimits` 兜底。
2. **确认框**：Discarder 要合并确认对话框 → 暗示原版 ThrowItem 有「确定要销毁吗」。需实测确认是否存在（决定要不要短路）。**下次问主人现在 E 键丢会不会弹框。**
3. **Fika 同步**：必须走原版事务入口（ItemUiContext.ThrowItem → TryRunNetworkTransaction），Fika host 才能 `ApplyThrowItemAsLoot` 广播。手搓 Remove 会 desync。

## 四、现有生态（无现成「丢弃区」成品）

- **tyfon7/Discarder**：多选丢弃 mod，依赖 UI Fixes；证实右键 Discard → `ItemUiContext.ThrowItem` 的 patch 点；合并确认框逻辑可抄
- **tyfon7/UIFixes**：主流 UI 平台，有 MultiSelect（圈选）、LimitDragPatches（右键/Shift 拖拽拦截）、GridHighlightPatches（拖拽高亮）；无丢弃区功能
- forge/ODDBA 未见 drop-zone 类成品

## 五、方案选项（下次向主人提问）

| 方案 | 说明 | 难度 |
|---|---|---|
| A. 丢弃区 + 可选热键 | InventoryScreen 上放丢弃区，patch OnEndDrag 检测松手落点 → ThrowItem；另配 config 热键 | 中 |
| B. 纯丢弃区 | 只做拖放 | 中 |
| C. 纯热键解耦 | 丢弃绑别的键（最省事，仍有误触/占用风险） | 低 |

**推荐 A**。待决策点：①方案选型 ②现在 E 键丢弃是否弹确认框 ③范围（仅 raid / 含 hideout 仓库）④Fika 场景为主？

## 参考源
- 本地程序集：`D:\free games\EFT_0821\EscapeFromTarkov_Data\Managed\Assembly-CSharp.dll`（SPT patched, 2026-09-08）；解析脚本 E:\JUST_DO_IT\tmp\tk_*.py（dnfile, Python311）
- 反编译仓库：PulledP0rk/Cookie（ItemUiContext.cs 3463 行, InteractionsHandlerClass.cs 4105 行）、itinybad/SPT-Client-400、Luna-Salamanca/assemblycsharptarkovspt4
- 实 mod 源码：tyfon7/Discarder（src/Patches/DiscardPatches.cs）、tyfon7/UIFixes（LimitDragPatches.cs, GridHighlightPatches.cs）
- SPT 服务器端：sp-tarkov/server-csharp 无 discard → 限额纯客户端
